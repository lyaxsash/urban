export default function (a, b) { // default потмоу что мы пишем, что из этого файла будет только эта функция. В таком случае ей можно не давать названия sum, а дать его уже при импорте
	return a+b;
}

